$(document).ready(function () {
    // make the lightbox automatically scroll
    setInterval(() => {
        $(".lightbox").scrollLeft($(".lightbox").scrollLeft() + 1);
    }, 20);
});

$(function () {

});